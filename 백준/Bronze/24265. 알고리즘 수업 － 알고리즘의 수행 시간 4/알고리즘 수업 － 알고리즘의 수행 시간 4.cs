long n = long.Parse(Console.ReadLine());
            long s = 0;

            for (int i = 1; i <= n - 1; ++i)
                s += i;

            Console.WriteLine(s);
            Console.WriteLine(2);